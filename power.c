#include<stdio.h>
int main()
{
    int x,y,result;
    printf("enter the value of x and y :");
    scanf("%d %d",&x,&y);
    result=pow(x,y);
    printf("the result is =%d",result);






    return 0;
}
